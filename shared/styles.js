import {StyleSheet } from 'react-native';

export const colors = {
    dark: '#1e265c',
    light: '#ffffff',
    golden: '#ffaa51',
    grayish: '#c6c8d6'
}

export const basic = StyleSheet.create({
    container: {
        padding: 40,
        flex: 1,
        justifyContent: 'center'
    }
})